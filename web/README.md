# XWGen Studio

The React constructor UI for XWGen. It runs alongside `web_server.py`, which
exposes the existing Python CSP solver and word index to the browser.

From the repository root, run `./run_web.ps1` to start both services. See the
root README for setup, usage, and solver documentation.
